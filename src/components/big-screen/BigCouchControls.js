import React from 'react';
import {Button} from "@mui/material";
import './BigCouchControls.css';

const BigCouchControls = () => {
    return(
        <div className="big-couch-controls">
            <Button variant="contained" size="large" color="primary">
                button
            </Button>
        </div>
    )
}

export default BigCouchControls;